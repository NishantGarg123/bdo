# Django — local overrides (optional)
# Root .env is loaded automatically for Docker and shared settings.
# Copy from ../.env.example or set USE_SQLITE=True for local dev without Docker.

USE_SQLITE=True
DB_HOST=localhost

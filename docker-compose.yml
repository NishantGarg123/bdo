services:
  db:
    image: postgres:16-alpine
    container_name: bdo_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: bdo_leads
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"